@model IEnumerable<BizSparkSupport.DAL.Case>

@foreach (var item in Model) {
    @Html.ActionLink((Html.DisplayFor(modelItem => item.Subject)).ToString(), "CaseDetails", "Case", new { caseId = item.CaseID }, new { @class = "btn btn-primary", @style = "margin-right:5px;" }) 
}
