@model IEnumerable<BizSparkSupport.DAL.Case>

@{
    ViewBag.Title = "StartUpCases";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container" style="margin-top: 20px;">
    <h2>@ViewBag.StartUpName</h2>
    <table id="example" class="table table-striped table-bordered nowrap" cellspacing="0" width="100%">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Subject)
            </th>

            <th>
                @Html.DisplayName("Date Arrived")
            </th>

            <th>
                @Html.DisplayName("Category")
            </th>
            <th>
                @Html.DisplayName("Priority")
            </th>
            @*<th>
                    @Html.DisplayName("Case Detail")
                </th>*@


        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Subject)
                </td>

                <td>
                    @Html.DisplayName(item.SubmissionDate.ToShortDateString())
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.Category.CategoryName)
                </td>
                <td style="color:@item.Priority.PriorityColor">
                    @Html.DisplayFor(modelItem => item.Priority.PriorityName)
                </td>
                @*<td>
                        @Html.ActionLink("Show Case", "CaseDetails", "CategoryAdmin", new { caseId = item.CaseID }, null)

                    </td>*@


            </tr>
        }

    </table>
</div>